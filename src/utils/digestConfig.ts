// scripts/digest/config.json の型付きラッパー
const config = {
    keywords: ['TypeScript', 'React', '設計', 'パフォーマンス', 'セキュリティ', 'LLM', 'AI'],
    topics: ['typescript', 'react', 'aws', 'llm'],
} as const;

export default config;
